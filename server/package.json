{
  "name": "maoyouji-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --exclude \"test/db/**\"",
    "test:db": "vitest run test/db"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "mysql2": "^3.14.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
